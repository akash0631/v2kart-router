{
  "name": "v2kart-router",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:set": "wrangler kv key put --binding=PINCODE_MAP",
    "kv:get": "wrangler kv key get --binding=PINCODE_MAP",
    "kv:list": "wrangler kv key list --binding=PINCODE_MAP"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
